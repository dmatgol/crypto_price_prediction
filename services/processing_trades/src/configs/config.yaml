exchanges:
  - name: kraken
    channels:
      - trade

product_ids:
  - coin: "ETH-USD"
    aggregation:
      type: "volume"
      interval: 40
  - coin: "BTC-USD"
    aggregation:
      type: "volume"
      interval: 10
  - coin: "LTC-USD"
    aggregation:
      type: "volume"
      interval: 100
  - coin: "XRP-USD"
    aggregation:
      type: "volume"
      interval: 40000
