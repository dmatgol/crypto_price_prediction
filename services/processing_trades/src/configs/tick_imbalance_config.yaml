exchanges:
  - name: kraken
    channels:
      - trade

product_ids:
  - coin: "ETH-USD"
    aggregation:
      type: "tick imbalance"
      interval: 80
  - coin: "BTC-USD"
    aggregation:
      type: "tick imbalance"
      interval: 120
  - coin: "LTC-USD"
    aggregation:
      type: "tick imbalance"
      interval: 50
  - coin: "XRP-USD"
    aggregation:
      type: "tick imbalance"
      interval: 60
