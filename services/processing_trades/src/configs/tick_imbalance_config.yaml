exchanges:
  - name: kraken
    channels:
      - trade

product_ids:
  - coin: "ETH-USD"
    aggregation:
      type: "tick imbalance"
      interval: 250
  - coin: "BTC-USD"
    aggregation:
      type: "tick imbalance"
      interval: 500
  - coin: "LTC-USD"
    aggregation:
      type: "tick imbalance"
      interval: 60
  - coin: "XRP-USD"
    aggregation:
      type: "tick imbalance"
      interval: 100
