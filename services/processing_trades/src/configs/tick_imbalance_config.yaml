exchanges:
  - name: kraken
    channels:
      - trade

product_ids:
  - coin: "ETH-USD"
    aggregation:
      type: "tick imbalance"
      interval: 20
  - coin: "BTC-USD"
    aggregation:
      type: "volume"
      interval: 30
  - coin: "LTC-USD"
    aggregation:
      type: "volume"
      interval: 15
  - coin: "XRP-USD"
    aggregation:
      type: "volume"
      interval: 10
