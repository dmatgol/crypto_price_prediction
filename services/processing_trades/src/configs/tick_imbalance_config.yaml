exchanges:
  - name: kraken
    channels:
      - trade

product_ids:
  - coin: "ETH-USD"
    aggregation:
      type: "tick imbalance"
      interval: 5
  - coin: "BTC-USD"
    aggregation:
      type: "tick imbalance"
      interval: 5
  - coin: "LTC-USD"
    aggregation:
      type: "tick imbalance"
      interval: 5
  - coin: "XRP-USD"
    aggregation:
      type: "tick imbalance"
      interval: 5
